# Notes

- 2026-04-24: Created investigation folder `string-permutation-kata-review`.
- 2026-04-24: Starting review of local `string-permutation-kata` repository for behavioral bugs.
- 2026-04-24: Confirmed `string-permutation-kata` is part of the top-level research Git repo, not a separate repository.
- 2026-04-24: Read kata README plus Rust source files under `string-permutation-kata/rust/src`.
- 2026-04-24: Existing test suite initially blocked on crates.io DNS from sandbox; reran `cargo test` with network approval.
- 2026-04-24: Baseline verification passed: 11 integration tests passed in `string-permutation-kata/rust`.
- 2026-04-24: Started targeted edge-case review against the stated contract: string/char semantics, enabled operations, distinct output, and ordering.
- 2026-04-24: Added external repro crate under `string-permutation-kata-review/repro` so the kata repository remains unchanged.
- 2026-04-24: Repro test `unicode_seed_does_not_break_candidate_enumeration` fails: `enumerate_candidates` returns invalid UTF-8 for valid input seed `"é"`.
- 2026-04-24: Repro test `invalid_utf8_alphabet_is_rejected_before_search` fails: `SearchConfig::new` accepts `0xff` in the byte alphabet even though output type is `String`.
- 2026-04-24: Root cause for both repro failures: `SearchConfig` stores `alphabet: Arc<[u8]>`, mutation generation edits byte offsets, and `enumerate_candidates` converts generated byte vectors into `String` only at output time.
- 2026-04-24: Resolution — migrated kata to `Vec<char>`-based API (alphabet, keyboard, mutation scratch, visited set, layer-best map). Added `EnabledOperations` with a `with_enabled_operations` builder. Repro tests rewritten against the new API and now pass; equivalent regression coverage added to the kata's own test suite.
